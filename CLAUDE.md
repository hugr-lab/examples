# examples Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-02

## Active Technologies
- GraphQL SDL (schema), Markdown (documentation) + hugr platform, Microsoft Fabric Warehouse (cloud service) (002-fabric-warehouse)
- Microsoft Fabric Warehouse (external, cloud-managed via `azure://` URI with `mssql` type) (002-fabric-warehouse)

- Bash (setup scripts), GraphQL SDL (schema) + Docker, SQL Server 2022, sqlcmd CLI (001-mssql-adventure-works)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Bash (setup scripts), GraphQL SDL (schema)

## Code Style

Bash (setup scripts), GraphQL SDL (schema): Follow standard conventions

## Recent Changes
- 002-fabric-warehouse: Added GraphQL SDL (schema), Markdown (documentation) + hugr platform, Microsoft Fabric Warehouse (cloud service)

- 001-mssql-adventure-works: Added Bash (setup scripts), GraphQL SDL (schema) + Docker, SQL Server 2022, sqlcmd CLI

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
